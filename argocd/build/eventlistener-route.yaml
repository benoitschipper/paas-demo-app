apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: paas-demo-app-listener
  namespace: example-paas-tekton
spec:
  to:
    kind: Service
    name: el-paas-demo-app-listener
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

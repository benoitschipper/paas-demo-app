apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: paas-demo-app-listener
  namespace: example-paas-tekton
spec:
  serviceAccountName: pipeline
  triggers:
    - name: github-push
      bindings:
        - ref: paas-demo-app-binding
      template:
        ref: paas-demo-app-template

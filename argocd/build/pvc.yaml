apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paas-demo-pipeline-workspace
  namespace: example-paas-tekton
  labels:
    app: paas-demo-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

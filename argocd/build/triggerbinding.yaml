apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: paas-demo-app-binding
  namespace: example-paas-tekton
spec:
  params:
    - name: GIT_REVISION
      value: $(body.after)
    - name: GIT_URL
      value: $(body.repository.clone_url)
